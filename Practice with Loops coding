
function setup() {
  createCanvas(400, 400);
}
var x=x+3


function draw() {

  background(255);

  colorMode(HSB, 400, 400, 100);

  fill(x, 204, 100);
  for (var x = 50; x < width - 30; x = x + 20) {
    line(x, height / 2, mouseX, mouseY);

  }
}
